name: Poll YouTube for Live Streams

on:
  schedule:
    - cron: '0 8-20 * * *'  # At the start of every hour from 8am to 8pm
    - cron: '15 8-20 * * *'  # At 15 minutes past every hour from 8am to 8pm
    - cron: '30 8-20 * * *'  # At 30 minutes past every hour from 8am to 8pm
    - cron: '45 8-20 * * *'  # At 45 minutes past every hour from 8am to 8pm
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: write
  pull-requests: none
  actions: write

jobs:
  poll-youtube:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib atproto
    
    - name: Create data directory
      run: mkdir -p data
    
    - name: Update streams data
      env:
        GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
      run: python script/update_streams.py
    
    - name: Check if streams.json changed
      id: check_changes
      run: |
        git add data/streams.json
        if ! git diff --cached --quiet; then
          echo "changed=true" >> "$GITHUB_OUTPUT"
        fi

    - name: Commit and push if changed
      if: steps.check_changes.outputs.changed == 'true'
      env:
        BLUESKY_USERNAME: ${{ secrets.BLUESKY_USERNAME }}
        BLUESKY_PASSWORD: ${{ secrets.BLUESKY_PASSWORD }}
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git commit -m "Update streams data"
        git push
        python script/post_to_bluesky.py 

    - name: Trigger deploy workflow
      if: steps.check_changes.outputs.changed == 'true'
      uses: actions/github-script@v7
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          await github.rest.actions.createWorkflowDispatch({
            owner: context.repo.owner,
            repo: context.repo.repo,
            workflow_id: 'deploy.yml',
            ref: 'main'
          })
